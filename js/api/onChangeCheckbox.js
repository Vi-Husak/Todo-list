// import { tasks } from "./tasksData.js";

// export default (id, target) => {
//   const index = tasks.findIndex((task) => task.id === id);
//   if (index !== -1) {
//     tasks[index].checked = target.checked;
//     localStorage.setItem("tasks", JSON.stringify(tasks));
//   }
// };
